# DashScope compatible Qwen API key. Keep this file local and never commit the real key.
DASHSCOPE_API_KEY=
# Optional override, defaults to qwen-3.6-plus
QWEN_MODEL=qwen-3.6-plus
# Set to 0 only if you need to disable the coordinate grid sent to Qwen.
QWEN_GRID=1
