# Draft ReFormat

Draft ReFormat 是一个本地运行的多模态草稿分割与终稿排版工作台。它适合把试卷、讲义、网页截图、化学结构式、生物图片和带批注的草稿整理成可继续编辑的终稿。

应用由两部分组成：浏览器中的长画布编辑器，以及负责调用 Qwen 多模态模型的本地 Python 服务。图片只在执行 AI 分析时发送到配置的 Qwen 兼容接口；编辑、裁剪、排布和项目 JSON 保存都在本地浏览器完成。

## 主要能力

- 多素材队列：批量上传 PNG、JPG、WEBP，也可以导入 HTML/MHTML 或网页图片资源。
- 语义分割：识别文字、图片、表格、公式、化学结构式、生物图片、图注和修正内容。
- 多边形裁剪：区域边界只能使用多边形，矩形也必须作为多边形处理；不再把外接框发送给模型。
- 多边形挖空：区域内部删除部分也使用多边形，并会绘制在监督/修改 agent 的参考图上，最终裁剪时按多边形涂白。
- 化学语义保护：尽量保持完整的反应式、机理、箭头、条件、催化剂和连接标注，不从语义单元中间拆开。
- 删除与修正：圈选、划线删除内容会单独标记；邻近修正作为独立区域处理。
- 监督审校：初次分割后由监督 agent 检查漏块、误合并、边缘完整性和语义完整性；失败时由修改 agent 重建完整区域列表，再交回监督 agent 复核，最多 10 轮。
- 单块细分：选中一个内容块后点击“分析并细分此块（保留原位）”。子块会映射回原图坐标，保持父块中的相对位置，替换父块时只收拢外围空白，不删除任何子块内容。
- 长画布：所有素材上下串接，块可以自由拖动并跨越页面查看；支持缩放、画笔涂白、方形覆盖、删除块和调整图层顺序。
- 集合分页：使用编号集合把题目、图片、图注和相关内容绑定在一起；导出分页时尽量保证同一集合不被拆开。
- 纯内容导出：PDF 预览使用零页边距，不包含编辑器 UI 外壳；小块保持原始比例，不会为了撑满页面而放大。
- 离线演示：没有 API key 或请求失败时仍可启动界面并使用演示分区继续手动编辑。

## 快速启动

需要 Python 3.9 或更高版本。

```powershell
python -m pip install -r requirements.txt
Copy-Item .env.example .env
notepad .env
python server.py
```

然后打开：

```text
http://127.0.0.1:8765
```

Linux/macOS 可使用等价的 `cp .env.example .env` 命令。停止服务时在运行窗口按 `Ctrl+C`。

## Qwen 配置

`.env` 只保存在本地，不要提交真实 API key。推荐配置如下：

```dotenv
DASHSCOPE_API_KEY=替换为你的key
QWEN_MODEL=qwen3.8-flash
QWEN_ENDPOINT=https://ws-yqr3lqq5xyjbxf30.cn-beijing.maas.aliyuncs.com/compatible-mode/v1/chat/completions
QWEN_COORDINATE_FRAME=1
QWEN_SUPERVISOR_ROUNDS=10
QWEN_REQUEST_TIMEOUT=180
```

配置项说明：

- `DASHSCOPE_API_KEY`：DashScope/MaaS 兼容接口的访问密钥。也兼容读取 `QWEN_API_KEY`。
- `QWEN_MODEL`：默认是 `qwen3.8-flash`，可改为工作空间实际开放的模型 ID。
- `QWEN_ENDPOINT`：OpenAI 兼容的 chat completions 地址。当前默认使用工作空间的兼容模式地址。
- `QWEN_COORDINATE_FRAME`：为 `1` 时给初次分割图附加外侧 0–100 数值坐标轴；不会绘制内部网格线。
- `QWEN_SUPERVISOR_ROUNDS`：监督轮数配置，服务端硬上限为 10，超过 10 会自动截断。
- `QWEN_REQUEST_TIMEOUT`：单次模型请求超时时间，单位为秒。

可以用健康检查确认服务读取到了模型配置：

```text
http://127.0.0.1:8765/api/health
```

返回内容不会包含 API key，只会报告模型名称、是否配置 key 和最近一次错误摘要。

## AI 分割流程

1. 初次分割 agent 接收原图和外侧数值坐标轴，返回完整 `regions` 列表。
2. 服务端拒绝缺少多边形、带有 `x/y/w/h` 或 `bbox` 的模型输出。
3. 服务端根据多边形顶点推导内部裁剪尺寸，供浏览器裁剪和排版使用；这些派生字段不会再发送给模型。
4. 监督和修改阶段各接收两张对齐图片：原比例候选边界图，以及带外侧数值坐标轴的放大图。
5. 区域边界使用紫色/红色多边形，删除区域使用红色多边形，内部挖空使用橙色多边形；图片中不绘制候选外接框。
6. 监督通过后才将最终区域交给编辑器。监督失败时，修改 agent 根据问题和当前多边形重新输出完整区域列表。

模型面对的区域结构大致如下：

```json
{
  "id": "r1",
  "label": "Reaction Pathway",
  "kind": "chemistry",
  "polygon": [[12.5, 8.2], [46.5, 8.2], [42.0, 29.7], [15.0, 29.7]],
  "group": "Q1",
  "description": "Complete reaction with conditions",
  "editAction": "keep",
  "holes": [
    {"polygon": [[20.0, 15.0], [26.0, 14.0], [30.0, 16.0], [28.0, 20.0], [22.0, 21.0]]}
  ]
}
```

`polygon` 和 `holes[].polygon` 都使用相对于原图内容区的 0–100 百分比坐标。模型不得输出矩形边界字段。编辑器内部保留 `x/y/w/h` 只是为了裁剪尺寸和排版计算，不属于模型协议。

## 单块细分

单块细分用于初次分割已经找到大语义块，但需要进一步拆开其中的题号、图注、结构式或修正内容的情况：

1. 在中央画布点击目标块。
2. 在右侧检查器点击“分析并细分此块（保留原位）”。
3. 服务端将当前多边形块裁成临时图片，重新调用初次分割、监督和修改流程。
4. 子块坐标按父块坐标映射回原图；子块继承父块的编号集合，并在画布中保留父块内的相对位置。
5. 只有在得到至少两个有效子多边形并通过监督后，才会替换父块；失败时原块保持不变。

## 完整使用教程

### 1. 导入和整理素材

1. 点击左侧“拖入图片，或点击上传”区域，或画布空状态中的“选择图片”，选择一张或多张 PNG、JPG、WEBP 图片。
2. 也可以在“从网页导入”输入框粘贴网页地址后点击“导入”，或直接按 `Enter`。应用会提取页面中最多 6 张图片并加入素材队列。
3. 本地网页文件可通过“上传 HTML / MHTML 文件”导入。内嵌图片可以直接读取；无法访问的相对路径资源可能会被忽略。
4. 在素材队列中点击卡片可切换当前素材；拖动卡片、点击 `↑`/`↓`，或使用排序按钮调整素材顺序。重排时，同一来源页的块会一起移动并归回该页的相对位置。

> 上传区域当前以点击选取文件为准；界面上的“拖入图片”和底部“空格平移画布”提示尚未绑定独立交互。

### 2. 调用 AI 分割

1. 选择素材卡后点击“分析当前图片”。图片会先交给分割 agent，再等待监督/修改循环结束；最终审校完成前不会提前应用初分结果。
2. 多张图可点击“批量分析素材队列”。前端会并发提交所有尚未分析的本地图片，而不是逐张串行等待。
3. 处理阶段可在画布底栏的“处理进度”查看当前状态，例如提交图片、监督审校、裁剪第几张素材以及生成了多少页。
4. 分割结果出现后，打开“边界顶点”核对多边形轮廓。重点检查文字笔画、化学键、箭头、反应条件、图注和图片边缘是否完整。
5. 若整体正确但某个块过大，可单选它并点击“分析并细分此块（保留原位）”；若块的语义正确但边界不准，则点击“原位重分析此块”。

无法直接发送的远程图片会使用离线演示分区；批量分析中的请求失败项也会以演示分区占位，便于继续人工处理。提示条会明确标出，不应把演示块当作真实识别结果；当前图片的本地图片请求失败时则会直接报错，不会静默替换结果。

### 3. 选择、移动和人工修正

- 单选：切换到“选择”，点击一个内容块。
- 多选：按住 `Shift` 点击可加入或移出选择；也可在画布空白处按住拖动框选相交的多个块。
- 移动：在选中块上按住拖动。多选状态下会整体移动，位置可跨越原素材页面。
- 人工拆块：切换到“多边形分区”，在已有块内部按住并沿目标边缘连续描绘，松开后自动闭合。新块会从父块中抠出，父块相应位置形成多边形挖空；轨迹超出父块时会沿原模型边缘裁切。
- 画笔涂白：切换到“块内涂白”，在目标块上按住拖画。白色笔迹只属于该块，并会进入导出结果。
- 矩形覆盖：切换到“块内覆盖”，在目标块上从一个角拖到对角，生成白色矩形覆盖。
- 精修边界：单选块后，在右侧修改各顶点的 X/Y 百分比坐标；无效、自交或面积过小的多边形不会被接受。

画布是纵向长画布，可使用鼠标滚轮或右侧滚动条查看下方内容。缩放只改变编辑视图，不会把小块放大写入导出结果。

### 4. 设置集合和层级

同一题目的题干、配图、结构式、图注和答案应填写相同的“编号集合”。导出时，集合出现顺序以长画布上第一个块的位置为准，集合内部保留画布中的相对位置。若下一个块使用不同集合编号，当前实现会换到新页。

块之间发生覆盖时，通过“置底”“下移”“上移”“置顶”调整层级。多选时只能整体拖动或删除，语义、集合、顶点和层级等单块属性会禁用。

### 5. 预览并导出 PDF

1. 在右下“分页尺寸”中选择 A4、A3 或 Letter 竖版。
2. 保持“集合不拆页”开启时，同一集合单独组织为一页；关闭后，每个块会作为独立分页单元。
3. 输入页边距，允许范围为 0–120 px，默认 0。希望最大化有效面积时保持为 0。
4. 点击右下“生成分页预览”或顶栏“导出终稿”。应用会依次裁剪素材、按长画布顺序整理集合，并打开纯内容打印页。
5. 浏览器打印窗口中选择“另存为 PDF”。如果没有新窗口，请允许当前站点弹出窗口后重试。

导出页不包含编辑器 UI。每个块保持原始宽高比和画布尺寸；只有整个集合大于纸张可用区域时才会等比缩小，不会为了铺满纸张而放大。

## 按钮与控件详解

### 顶栏

| 按钮/控件 | 作用 | 使用说明 |
| --- | --- | --- |
| 项目名右侧 `⌘` | 重命名项目 | 在弹窗中输入名称；该名称用于界面标题和下载文件名。 |
| “自动保存已开启” | 当前为状态显示 | 当前版本没有后台持久化或自动恢复逻辑；重要结果请主动点击“保存项目”。 |
| 撤销 | 撤销最近一次可记录编辑 | 支持删除、移动、排序、属性修改、涂白等操作；最多保留 35 个历史快照。 |
| 重做 | 恢复刚撤销的编辑 | 执行新的编辑后，原重做队列会清空。 |
| 保存项目 | 下载项目 JSON | 下载 `.draft.json` 状态快照，并移除可重新生成的临时 `cropSrc`。当前界面尚无“打开项目”按钮，因此它不是一键恢复入口。 |
| 导出终稿 | 生成打印预览 | 与右下“生成分页预览”作用相同；随后可用浏览器保存 PDF。 |

快捷键：`Ctrl+Z`/`Cmd+Z` 撤销，`Ctrl+Shift+Z`/`Cmd+Shift+Z` 重做，`Esc` 返回“选择”工具并关闭内容类型菜单。

### 左侧输入素材与素材队列

| 按钮/控件 | 作用 | 使用说明 |
| --- | --- | --- |
| 上传区域 / 选择图片 | 添加图片 | 支持 PNG、JPG、WEBP 和多选；不会自动开始 AI 分析。 |
| 网页地址输入框 | 输入来源网页 | 粘贴 URL 后可按 `Enter`，等同点击“导入”。 |
| 导入 | 导入网页图片 | 服务端读取网页标题及图片地址，再代理可访问图片。 |
| 上传 HTML / MHTML 文件 | 从本地网页文件提取图片 | 优先读取内嵌 data URL；外部或本地相对资源不一定可访问。 |
| 按编号排序 | 按名称中的数字自然排序 | 例如 `2.jpg` 会排在 `10.jpg` 前；排序会写入撤销历史。 |
| 按名称排序 | 按中文文件名排序 | 使用普通名称比较，不启用数字自然排序。 |
| 素材卡片 | 切换当前素材 | 当前素材决定“分析当前图片”的目标；卡片本身可拖动排序。 |
| `↑` / `↓` | 上移/下移素材 | 第一项不能继续上移，最后一项不能继续下移。 |
| 素材卡 `×` | 删除素材 | 同时移除该素材的分块；可立即使用“撤销”恢复。 |
| 分析当前图片 | 分析当前素材 | 提交分割并持续等待最多 10 轮监督流程完成，然后一次性显示最终块。 |
| 批量分析素材队列 | 并发分析队列 | 只处理尚未分析且可作为 data URL 发送的图片。 |
| AI 卡片右上开关 | 当前为状态装饰 | 尚未绑定点击行为；不要把它当作持久化或 API 调用开关。 |
| 左下 `?` | 预留帮助入口 | 当前版本尚未绑定操作。 |

### 中央画布工具栏和底栏

| 按钮/控件 | 作用 | 使用说明 |
| --- | --- | --- |
| 选择 | 选择和移动块 | 点击单选，`Shift` 点击或空白拖框多选；在块上拖动可整体移动当前选择。 |
| 多边形分区 | 从现有块抠出区域 | 必须从内容块上开始，按住连续描边，松开后闭合；父块留下多边形 hole，新区域保持原位。 |
| 块内涂白 | 自由画笔涂白 | 必须直接在目标块上拖画；当前没有独立笔宽设置。 |
| 块内覆盖 | 矩形涂白 | 在目标块上拖出覆盖范围。这里的矩形只是编辑涂白工具，不是 AI 分割边界。 |
| 边界顶点 | 显示/隐藏多边形参考线 | 同时控制模型外轮廓、挖空轮廓和 V1、V2…顶点编号，只影响编辑显示。 |
| `−` / `＋` | 缩小/放大画布 | 每次变化 10%，范围为 65%–160%。 |
| `⌗` | 恢复 100% | 当前实现将缩放恢复为 100%，并非自动适应窗口。 |
| 清除当前涂白 | 清除单个块的涂白 | 需要先单选一个块；同时清除该块的画笔笔迹和矩形覆盖，不影响 AI holes。 |
| 删除选中 | 删除内容块 | 支持单选和多选，可通过“撤销”恢复。 |
| 处理进度 | 只读状态 | 显示分析、监督、裁剪和分页进度，不是按钮。 |

### 右侧属性检查器

| 按钮/控件 | 作用 | 使用说明 |
| --- | --- | --- |
| 语义标签输入框 | 修改块名称 | 输入完成并失焦/确认后生效。 |
| 正文说明 / 图表・图片 / 化学结构式 | 快速填写常用标签 | 只修改显示标签，不改变内容类型。 |
| 内容类型 | 修改语义类别 | 可选文字段落、图片/插图、表格、化学结构式、生物图片、数学公式。 |
| 编号集合输入框 | 修改集合 ID | 相关内容应使用完全相同的 ID，例如 `Q3`。 |
| 新建 | 分配新的集合 ID | 自动生成当前项目尚未使用的集合编号。 |
| 分析并细分此块（保留原位） | 把一个块拆成多个子多边形 | 发送当前裁剪块进行独立分割和监督；至少得到两个有效子块才替换父块，失败时父块不变。子块继承父集合和相对位置。 |
| 原位重分析此块 | 只校正当前边界 | 将原图、现有多边形和 holes 发送给专用重分析接口；不拆分内容。等待期间若用户改过几何，结果不会覆盖新编辑。 |
| 添加顶点 | 在最长边中点插入顶点 | 新顶点随后可通过 X/Y 输入框精调。 |
| 顶点 X/Y | 修改原图百分比坐标 | 范围 0–100；每次修改都会同步更新裁剪范围和块尺寸。 |
| 顶点行 `×` | 删除该顶点 | 多边形最少保留 3 个顶点；三角形时按钮禁用。 |
| 置底 / 下移 / 上移 / 置顶 | 调整块的图层顺序 | 用于控制相交块的覆盖优先级。 |
| 移除该内容块 | 删除当前块 | 多选时按钮会变为“移除选中的 N 个内容块”。 |
| AI 置信度 | 只读参考信息 | 展示模型返回值及描述，不可编辑，也不代表监督是否通过。 |
| 右上 `•••` | 预留菜单入口 | 当前版本尚未绑定操作。 |

### 终稿设置

| 按钮/控件 | 作用 | 使用说明 |
| --- | --- | --- |
| 分页尺寸 | 选择纸张 | 支持 A4、A3、Letter 竖版。 |
| 集合不拆页 | 控制分页分组 | 开启时相同集合放在同一页；关闭时每个块独立成页。 |
| 页边距 | 设置内容距页面边缘的距离 | 单位 px，范围 0–120，默认 0。 |
| 生成分页预览 | 打开打印页面 | 生成纯内容 HTML，并自动唤起浏览器打印对话框。 |

## 本地接口

服务端只提供本地编辑器需要的轻量接口：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/health` | 查看服务、模型和 key 配置状态 |
| POST | `/api/segment` | 提交一张图片进行初次分割；返回初始区域和异步监督任务 ID |
| GET | `/api/segment-status?jobId=...` | 查询监督任务状态和最终区域 |
| POST | `/api/import-url` | 读取网页标题和图片资源地址 |
| POST | `/api/proxy-image` | 将网页图片代理为浏览器可用的 data URL |

`/api/segment` 的请求体示例：

```json
{
  "image": "data:image/jpeg;base64,...",
  "hint": "保持题号、图注和化学反应的语义完整"
}
```

## 测试与模型基准

运行回归测试：

```powershell
python -m unittest -v
python -m py_compile server.py benchmark_qwen.py test_server.py
node --check public/app.js
git diff --check
```

运行模型对比脚本：

```powershell
python benchmark_qwen.py
```

脚本会对同一张合成测试图依次请求 `qwen3.8-flash`、`qwen3.7-plus`、`qwen3.6-flash` 和 `qwen3.6-plus`，输出耗时、请求状态、区域数量和多边形数量。脚本不会打印 API key。

## 安全与隐私

- 不要把真实 API key 写入 README、源码、提交记录或截图。
- `.env` 已加入 `.gitignore`；提交前请检查 `git status` 和 `git diff`。
- 上传的图片只会在 AI 分析请求时发送到 `QWEN_ENDPOINT`；本地编辑操作不会自动上传。
- 网页导入会读取网页中的图片资源，使用前请确认目标网页和图片包含的信息适合发送到所配置的模型服务。
- `1.jpeg` 等本地测试素材不属于程序配置，不应随代码提交，除非明确需要作为公开测试资源。

## 项目结构

```text
DraftReFormat/
├─ server.py              # 本地 HTTP 服务、Qwen 调用、坐标和监督校验
├─ public/
│  ├─ index.html          # 编辑器页面结构
│  ├─ styles.css          # 编辑器样式和长画布布局
│  └─ app.js              # 上传、裁剪、拖动、细分、分页和导出逻辑
├─ benchmark_qwen.py      # Qwen 模型对比脚本
├─ test_server.py         # JSON schema、监督轮数和多边形标注回归测试
├─ requirements.txt       # Pillow 依赖
├─ .env.example           # 本地配置模板
└─ .gitignore
```

## GitHub 发布前检查

```powershell
git status --short
git diff --check
git log -1 --stat
```

确认没有 `.env`、API key、个人图片或临时测试文件后，再提交和推送代码。
